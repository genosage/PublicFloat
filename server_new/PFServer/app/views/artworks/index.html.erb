<p id="notice"><%= notice %></p>

<h1>Listing Artworks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Artwork longtitude</th>
      <th>Artwork latitude</th>
      <th>Title</th>
      <th>Image url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @artworks.each do |artwork| %>
      <tr>
        <td><%= artwork.name %></td>
        <td><%= artwork.description %></td>
        <td><%= artwork.artwork_longtitude %></td>
        <td><%= artwork.artwork_latitude %></td>
        <td><%= artwork.title %></td>
        <td><%= artwork.image_url %></td>
        <td><%= link_to 'Show', artwork %></td>
        <td><%= link_to 'Edit', edit_artwork_path(artwork) %></td>
        <td><%= link_to 'Destroy', artwork, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Artwork', new_artwork_path %>
